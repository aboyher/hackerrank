file = open("names.txt")
names = file.read()[1:-1].split('","')
names.sort()
name = names[0]
value = []
for i,name in enumerate(names):
  namevals = []
  for ch in name:
    namevals.append(ord(ch)-64)
  value.append(sum(namevals)*(i+1))

print (sum(value))
